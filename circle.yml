machine:
  node:
    version: 0.10.13
deployment:
  production:
    branch: master
    heroku:
      appname: protractor-tests2
dependencies:
  pre:
    - wget -O selenium-server.jar http://selenium.googlecode.com/files/selenium-server-standalone-2.33.0.jar
  override:
    - npm install -dev

checkout:
  post:
    - git submodule update --init

test:
  pre:
    - npm install grunt
    - npm install node-dev
    - npm install jasmine-node@1.14.3

  override:
    - npm test
    - grunt protractor-test
