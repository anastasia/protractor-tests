machine:
  node:
    version:
      0.10.28

dependencies:
  override:
    - npm install -dev

checkout:
  post:
    - git submodule update --init

test:
  pre:
    - npm start:
        background: true
    - sleep 5

  override:
    - npm run protractor
